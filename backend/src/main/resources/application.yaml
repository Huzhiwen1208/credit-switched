server: 
  port: 8081

# Mail configuration - replace with your SMTP provider settings
spring:
  mail:
    host: smtp.163.com
    port: 465
    username: 18774183423@163.com
    password: MZrAm3BdQLTfBJHQ
    protocol: smtp
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
          ssl:
            enable: true
          socketFactory:
            port: 465
            class: javax.net.ssl.SSLSocketFactory
      # Enable the JavaMail debug output (prints the SMTP wire protocol conversation)
      debug: true

logging:
  level:
    org.springframework.mail: DEBUG

